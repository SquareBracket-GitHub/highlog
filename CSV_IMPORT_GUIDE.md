# Highlog CSV 과목 등록 가이드

Highlog의 CSV 등록 기능은 현재 파일 업로드 방식이 아니라, 관리자가 앱의 **CSV 일괄 등록** 화면에 CSV 형식의 텍스트를 붙여 넣는 방식입니다.

## 가장 빠른 등록 순서

1. 과목 관리 권한이 있는 계정으로 로그인합니다.
2. 하단 메뉴에서 **과목 선택** 화면으로 이동합니다.
3. 화면 상단의 **내 과목 관리**를 누릅니다.
4. **CSV 등록**을 누릅니다.
5. 입력란에 들어 있는 예시를 모두 지웁니다.
6. 헤더를 포함한 CSV 텍스트를 붙여 넣습니다.
7. 아래의 **미리보기**에서 모든 행이 정상인지 확인합니다.
8. **검증된 과목 일괄 등록**을 누릅니다.
9. 등록 완료 개수와 **내 과목 관리** 목록을 확인합니다.

**내 과목 관리**가 보이지 않으면 현재 로그인 계정에 과목 관리 권한이 없거나, DB에서 권한을 부여한 뒤 다시 로그인하지 않은 상태입니다.

## 등록 전 관리자 권한 준비

CSV 등록 화면은 `can_manage_courses` 권한이 있는 학생 계정만 사용할 수 있습니다. 개발 DB에서 관리자 후보 계정을 먼저 회원가입한 뒤 다음과 같이 권한을 부여할 수 있습니다.

```sql
UPDATE students
SET can_manage_courses = TRUE
WHERE login_id = '관리자로그인아이디';
```

앱이 로그인 당시 받은 사용자 정보로 메뉴 노출 여부를 판단하므로, DB에서 권한을 변경한 뒤에는 반드시 로그아웃하고 다시 로그인합니다.

일반 학생에게는 **내 과목 관리** 메뉴가 보이지 않으며, 직접 API를 호출해도 서버가 HTTP 403으로 거부해야 합니다.

## 기본 형식

첫 번째 줄은 헤더이며, 각 데이터 행은 정확히 8개 열로 작성합니다.

```csv
과목명,학년,반,태그,수업장소,일정,색상,반전체공통
```

| 열 | 필수 여부 | 설명 | 예시 |
|---|---|---|---|
| 과목명 | 필수 | 학생 화면에 표시할 과목명 | 물리학 |
| 학년 | 필수 | 대상 학년 | 2 |
| 반 | 조건부 | 반 전체 공통 과목의 대상 반. 선택과목은 비움 | 3 |
| 태그 | 조건부 | 선택과목 그룹. 반 전체 공통 과목은 비움 | 과학 선택 |
| 수업장소 | 필수 | 교실 또는 수업 장소 | 과학실 |
| 일정 | 필수 | `요일:교시`. 복수 일정은 `|`로 구분 | 월요일:2\|수요일:3 |
| 색상 | 필수 | 시간표 배경색인 6자리 HEX | `#BFDBFE` |
| 반전체공통 | 필수 권장 | `true`이면 고정 과목, `false`이면 선택과목 | false |

## CSV 텍스트 준비 방법

### 텍스트 편집기로 직접 작성

1. 메모장이나 VS Code에서 새 UTF-8 문서를 엽니다.
2. 표준 헤더를 첫 줄에 붙여 넣습니다.
3. 둘째 줄부터 과목을 한 줄에 하나씩 작성합니다.
4. 각 행에 쉼표가 정확히 7개인지 확인합니다.
5. 전체 텍스트를 복사해 앱의 CSV 입력란에 붙여 넣습니다.

```csv
과목명,학년,반,태그,수업장소,일정,색상,반전체공통
물리학,2,,과학 선택,과학실,월요일:2|수요일:3,#BFDBFE,false
```

### Excel 또는 Google Sheets에서 작성

스프레드시트에서는 열을 다음 순서로 만듭니다.

| A | B | C | D | E | F | G | H |
|---|---|---|---|---|---|---|---|
| 과목명 | 학년 | 반 | 태그 | 수업장소 | 일정 | 색상 | 반전체공통 |

1. 첫 행에 위 헤더 8개를 입력합니다.
2. 둘째 행부터 과목 데이터를 작성합니다.
3. 선택과목은 C열(반)을 비우고 D열(태그)을 입력합니다.
4. 반 전체 공통 과목은 C열을 입력하고 D열을 비웁니다.
5. F열의 복수 일정은 `월요일:2|수요일:3`처럼 작성합니다.
6. **CSV UTF-8(쉼표로 분리)** 형식으로 내보냅니다.
7. 저장된 `.csv` 파일을 UTF-8 텍스트 편집기로 엽니다.
8. 헤더를 포함한 전체 내용을 복사해 앱에 붙여 넣습니다.

Excel 셀 범위를 그대로 복사하면 환경에 따라 열 사이가 쉼표가 아닌 탭으로 복사될 수 있습니다. 현재 앱은 탭 구분 형식을 지원하지 않으므로 반드시 CSV UTF-8로 내보낸 결과를 사용하세요.

Google Sheets에서는 **파일 → 다운로드 → 쉼표로 구분된 값(.csv)** 순서로 내려받은 뒤, 내려받은 파일을 텍스트 편집기로 열어 복사합니다.

### PC에서 작성한 CSV를 Android 앱으로 옮기기

현재 앱은 파일 선택 기능이 없으므로 CSV 파일 자체가 아니라 파일 내용을 전달해야 합니다. 다음 중 편한 방법을 사용합니다.

- PC와 Android 사이의 클립보드 공유
- 본인만 접근할 수 있는 메모 또는 클라우드 문서
- 개발 중인 경우 Android 에뮬레이터의 클립보드 붙여 넣기

학생 개인정보나 운영 DB 비밀번호는 CSV에 포함하지 마세요. CSV에는 과목 정보만 들어갑니다.

## 선택과목

선택과목은 같은 학년 전체가 대상입니다.

- `반`은 비웁니다.
- `태그`를 입력합니다.
- `반전체공통`은 `false`로 입력합니다.
- 같은 태그의 과목은 대상 학년과 전체 일정이 모두 같아야 합니다.
- 학생은 같은 태그에서 과목 하나만 선택할 수 있습니다.

```csv
과목명,학년,반,태그,수업장소,일정,색상,반전체공통
물리학,2,,과학 선택,과학실,월요일:2|수요일:3,#BFDBFE,false
화학,2,,과학 선택,화학실,월요일:2|수요일:3,#FDE68A,false
```

위 예시에서 물리학과 화학은 모두 2학년 대상이며 일정도 같으므로 같은 `과학 선택` 그룹으로 등록할 수 있습니다.

## 반 전체 공통 과목

반 전체 공통 과목은 특정 학년·반 학생 모두에게 적용되는 고정 과목입니다.

- `반`을 입력합니다.
- `태그`는 비웁니다.
- `반전체공통`은 `true`로 입력합니다.
- 수업 장소는 반드시 입력합니다.

```csv
과목명,학년,반,태그,수업장소,일정,색상,반전체공통
국어,1,1,,1-1 교실,월요일:1|화요일:2,#BBF7D0,true
```

등록 결과:

- 대상 반의 기존 학생에게 `fixed` 수강이 자동 등록됩니다.
- 이후 대상 반에 가입하는 학생에게도 자동 등록됩니다.
- 과목 선택 목록에는 나타나지 않습니다.
- 시간표에는 과목명·장소·색상이 표시됩니다.

## 일정 작성법

일정 하나는 `요일:교시` 형식입니다.

```text
월요일:2
```

여러 일정은 `|`로 연결합니다.

```text
월요일:2|수요일:3|금요일:1
```

허용 범위:

- 요일: `월요일`, `화요일`, `수요일`, `목요일`, `금요일`
- 교시: `1`~`7`

잘못된 일정:

```text
월:2
토요일:1
월요일:8
월요일 2
월요일:2,수요일:3
```

쉼표는 CSV 열 구분자이므로 복수 일정을 구분할 때 사용할 수 없습니다.

## 반전체공통 값

현재 앱은 다음 값을 참으로 인식합니다.

```text
true
1
예
Y
```

그 밖의 값은 거짓으로 처리됩니다. 입력 실수를 줄이기 위해 항상 소문자 `true` 또는 `false`를 사용하는 것을 권장합니다.

## 색상 작성법

색상은 `#RRGGBB` 형태의 6자리 HEX 값이어야 합니다.

올바른 예:

```text
#BBF7D0
#BFDBFE
#FDE68A
```

잘못된 예:

```text
red
#FFF
BBF7D0
#GGFF00
```

## 등록 및 검증 과정

앱은 다음 순서로 CSV를 처리합니다.

1. 첫 번째 줄을 헤더로 간주해 제외합니다.
2. 나머지 행을 쉼표로 분리합니다.
3. 각 행이 정확히 8개 열인지 확인합니다.
4. 필수값, 색상, 일정, 반과 태그 조합을 검사합니다.
5. 행별 미리보기를 표시합니다.
6. 오류 행이 없으면 위에서부터 과목 생성 API를 순차 호출합니다.

서버는 각 행에 대해 다시 다음 항목을 검사합니다.

- 로그인 계정의 과목 관리 권한
- 필드 형식과 길이
- 월~금요일 및 1~7교시 범위
- 동일 시간표 슬롯 충돌
- 같은 태그 과목들의 학년과 전체 일정 일치
- 학년 공통 선택 슬롯과 반 전용 공통 슬롯 간 충돌

## 앱에서 등록하는 상세 절차

### 1. CSV 등록 화면 열기

1. 관리자 계정으로 로그인합니다.
2. 하단의 과목 선택 메뉴로 이동합니다.
3. **선택 과목** 화면 상단의 **내 과목 관리**를 누릅니다.
4. **내 과목 관리** 화면 상단의 **CSV 등록**을 누릅니다.
5. 화면 제목이 **CSV 일괄 등록**인지 확인합니다.

### 2. 예시 데이터 교체하기

CSV 입력란에는 사용법을 보여 주기 위한 예시 행이 기본으로 들어 있습니다. 예시를 남겨 둔 채 새 데이터를 덧붙이면 예시 과목도 함께 등록됩니다.

1. 입력란을 길게 누릅니다.
2. **전체 선택**을 누릅니다.
3. 준비한 CSV 텍스트를 붙여 넣습니다.
4. 첫 줄에 헤더가 있고 빈 첫 줄이 없는지 확인합니다.

### 3. 미리보기 확인하기

입력 내용이 바뀌면 화면 아래 미리보기가 즉시 갱신됩니다.

정상 행에는 다음 정보가 표시됩니다.

```text
2행 · 물리학 / 2학년 / 2개 일정
```

오류 행은 붉은 배경과 함께 원인을 표시합니다. 주요 오류는 다음과 같습니다.

- 열이 8개가 아님
- 필수값 또는 색상 형식 오류
- 반 공통 과목에 반이 없거나 태그가 있음
- 선택과목에 태그가 없거나 반이 입력됨
- 요일 또는 1~7교시 형식 오류

미리보기 제목의 괄호 안 숫자는 **정상으로 파싱된 과목 수**입니다. 원본 데이터 행 수와 이 숫자를 대조하세요. 오류 행이 하나라도 있으면 등록 버튼을 눌러도 등록이 시작되지 않습니다.

### 4. 등록 실행하기

1. 오류 행이 없는지 확인합니다.
2. **검증된 과목 일괄 등록**을 한 번만 누릅니다.
3. 버튼이 **등록 중...**으로 바뀌면 작업이 끝날 때까지 앱을 종료하거나 뒤로 이동하지 않습니다.
4. `N개 과목을 등록했습니다.` 안내가 표시되는지 확인합니다.
5. **확인**을 누르면 내 과목 관리 화면으로 돌아갑니다.

과목은 CSV 위쪽 행부터 한 개씩 등록됩니다. 행마다 서버가 시간 충돌과 태그 일관성을 다시 검사하므로, 미리보기가 정상이더라도 서버의 기존 데이터와 충돌하면 중간에 실패할 수 있습니다.

### 5. 등록 결과 확인하기

등록 직후 다음을 확인합니다.

1. 내 과목 관리 목록의 과목 수와 CSV 데이터 행 수가 같은지 확인합니다.
2. 각 과목의 학년, 대상 반 또는 학년 공통 여부, 장소와 전체 일정을 확인합니다.
3. 일반 학생 계정으로 로그인해 선택과목이 올바른 태그 아래 표시되는지 확인합니다.
4. 반 전체 공통 과목이 대상 반 학생의 시간표에 표시되는지 확인합니다.
5. 다른 학년이나 다른 반 학생에게 잘못 노출되지 않는지 확인합니다.

### 6. 일부 등록 실패 처리하기

`3개 등록 후 중단됐습니다`처럼 안내되면 앞의 3개 과목은 이미 저장된 상태입니다.

1. 오류 메시지에서 충돌 또는 입력 문제를 확인합니다.
2. 내 과목 관리 화면에서 실제로 등록된 과목을 확인합니다.
3. 원본 CSV를 복사해 별도 보관합니다.
4. 이미 성공한 행을 CSV에서 제거합니다.
5. 실패한 행을 수정합니다.
6. 남은 행만 다시 붙여 넣고 미리보기를 확인한 뒤 등록합니다.

전체를 그대로 다시 실행하면 앞서 성공한 과목과 일정 충돌이 발생할 수 있습니다. 중복 실행 전에 반드시 관리 목록과 성공 개수를 대조하세요.

## 오류 예시

선택과목인데 반을 입력한 경우:

```csv
물리학,2,1,과학 선택,과학실,월요일:2,#BFDBFE,false
```

반 전체 공통 과목인데 태그를 입력한 경우:

```csv
국어,1,1,국어 그룹,1-1 교실,월요일:1,#BBF7D0,true
```

허용 범위를 벗어난 교시:

```csv
물리학,2,,과학 선택,과학실,월요일:8,#BFDBFE,false
```

잘못된 색상:

```csv
물리학,2,,과학 선택,과학실,월요일:2,blue,false
```

## 현재 제한사항

### 전체 등록 트랜잭션 미지원

CSV 전체가 하나의 DB 트랜잭션으로 처리되지는 않습니다. 각 행을 순서대로 별도 등록합니다.

예를 들어 5개 행 중 네 번째 행에서 서버 충돌이 발생하면:

- 1~3번째 과목은 DB에 남습니다.
- 4번째 행에서 등록이 중단됩니다.
- 5번째 행은 시도하지 않습니다.
- 앞서 성공한 과목은 자동으로 취소되지 않습니다.

### 정식 CSV 따옴표 문법 미지원

현재 파서는 행을 단순히 쉼표로 분리합니다. 따라서 과목명이나 장소에 쉼표를 포함할 수 없으며, 따옴표로 감싸도 해결되지 않습니다.

지원되지 않는 예:

```csv
"통합과학, 실험",2,,과학 선택,과학실,월요일:2,#BFDBFE,false
```

쉼표가 필요한 값은 다른 문자로 바꿔야 합니다.

### 헤더 내용 검증 미지원

첫 번째 줄의 실제 문구는 검사하지 않고 무조건 제외합니다. 혼동을 막기 위해 문서의 표준 헤더를 그대로 사용하세요.

### 파일 업로드 미지원

현재 `.csv` 또는 `.xlsx` 파일을 직접 선택하는 기능은 없습니다. 파일 내용을 UTF-8 텍스트로 복사해 앱 입력란에 붙여 넣어야 합니다.

## 안전한 등록 절차

1. 같은 태그의 과목이 학년과 일정을 공유하는지 확인합니다.
2. 1~2개 행으로 먼저 시험 등록합니다.
3. 대량 등록 직전에 DB를 백업합니다.
4. 앱 미리보기에서 오류가 없는지 확인합니다.
5. 등록 완료 수와 관리 목록을 대조합니다.
6. 일부 등록 실패 시 이미 등록된 과목을 확인한 뒤 나머지만 다시 실행합니다.

DB 백업:

```powershell
.\scripts\backup-database.ps1
```

백업과 복구에 관한 자세한 내용은 [`DB_BACKUP_RESTORE.md`](./DB_BACKUP_RESTORE.md)를 참고하세요.

## 전체 예시

아래 예시는 선택과목 두 개와 반 전체 공통 과목 한 개를 보여줍니다.

```csv
과목명,학년,반,태그,수업장소,일정,색상,반전체공통
물리학,2,,과학 선택,과학실,월요일:2|수요일:3,#BFDBFE,false
화학,2,,과학 선택,화학실,월요일:2|수요일:3,#FDE68A,false
국어,1,1,,1-1 교실,월요일:1|화요일:2,#BBF7D0,true
```
